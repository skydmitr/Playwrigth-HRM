name: e2eTest

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          check-latest: true

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run tests
        run: npm run test

      - name: Send Telegram Notification 🎉
        if: always()
        uses: appleboy/telegram-action@master
        with:
          token: ${{ secrets.TELEGRAM_TOKEN }}
          id: ${{ secrets.TELEGRAM_CHAT_ID }}
          message: |
            🚀 **Playwright Tests ${{ job.status }}**
            
            📊 **Проект**: ${{ github.repository }}
            🔧 **Бранч**: ${{ github.ref_name }}
            👤 **Автор**: ${{ github.actor }}
            📅 **Коммит**: ${{ github.sha }}
            
            📈 **Тесты**: Passed: ${{ steps.test.outputs.tests }} | Failed: ${{ steps.test.outputs.failures }}
            ⏱️ **Время**: ${{ steps.test.outputs.duration }}
            
            🔗 [View in GitHub](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
